/**
 * Plugin metadata constants.
 *
 * Keep these values aligned with the host application. The build embeds these constants; the host validates metadata at load time.
 */
export { ContentType, ContentWarning } from '../types/constants';
